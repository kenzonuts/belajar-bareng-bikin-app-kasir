# Database (SQLite for local Phase 01; switch to PostgreSQL when Docker is available)
DATABASE_URL="file:./dev.db"

# Backend
API_PORT=3001
API_HOST=0.0.0.0
NODE_ENV=development

# Frontend (Vite)
API_URL=http://localhost:3001
VITE_API_URL=http://localhost:3001

# Optional PostgreSQL (docker compose) — uncomment and set provider to postgresql later:
# DATABASE_URL=postgresql://kas:kas@localhost:5432/kas_stock?schema=public
